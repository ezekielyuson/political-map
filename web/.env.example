# Where to find the PGE API. Set this in Vercel's project settings
# (Environment Variables) so the deployed frontend can reach Fly.
# Local dev uses http://localhost:8000 by default.
NEXT_PUBLIC_PGE_API_URL=https://political-map.fly.dev
